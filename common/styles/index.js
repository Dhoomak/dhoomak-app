import { StyleSheet } from "react-native"

const commonStyles = StyleSheet.create({
    shadow: {
        overflow: 'hidden',
        shadowColor: 'black',
        shadowRadius: 7,
        shadowOpacity: 1,
    },
})

export default commonStyles